function binarySearch(arr, find) {

    let storeValue = 0;
    let midValue = Math.floor(arr.length / 2);
    let hi = arr.length - 1;
    let low = 0;

    While(low <= hi); {
        if (arr[midValue] < find) {
            low = midValue + 1;
        } else if (arr[midValue] > find) {
            hi = midValue - 1;
        } else ();
    }



}
